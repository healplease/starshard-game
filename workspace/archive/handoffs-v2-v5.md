# Handoffs — v2–v5 increments (archived)

Frozen. These are the handoffs for the **closed** increments **v2** (bonuses + modular refactor),
**v3** (knowledge-base reorg), **v4** (QA docs), and **v5** (three enemy types) — entries **10–33**,
moved out of the active log to keep `../shared/handoffs.md` lean. The active log holds only the most
recent increment; v1 (entries 1–9) is in `handoffs-v1.md`.

Format: `<n>. <YYYY-MM-DD> — FROM <role> TO <role> — <one-line task>`

---

— — — v2 increment (bonuses + retiring the line cap) — — —

10. 2026-06-05 — FROM orchestrator TO business-analyst — v2 kickoff: define requirements for pickup BONUSES/power-ups (repair, spread/fan fire, rapid fire, shield/invuln — propose more if good) and record the architecture change (~500-line cap RETIRED → modular MVC-ish split of workspace/game/, smoke-test gate preserved). Append a dated v2 section to requirements.md.
11. 2026-06-05 — FROM business-analyst TO lead-game-designer — requirements.md v2 section added: R23–R33 MUST (bonus pickup entity, repair/spread/rapid/shield buffs, timed-drip+enemy-drop spawning, HUD timers, stacking/refresh, expiry+restart cleanup, modular MVC refactor, smoke gate preserved), R34/R35 COULD, C3 line cap RETIRED, AC14–AC21. Values delegated. Design the GDD v2: finalize the buff set + numbers + stacking and the module map.
12. 2026-06-05 — FROM lead-game-designer TO artist — GDD v2 complete: buff table locked (Repair +40 instant; Fan 3 beams ±12°/8 s; Rapid ×0.5 cooldown 12→6 f/8 s; Shield full-invuln 5 s; Score×2/10 s). R34 IN, R35 OUT. Stacking = independent timers + hard refresh-to-full, Repair never stacks, restart wipes all. Diamond pickup (26 px, r=13, 2.0 px/f, cap 3) + spawn defaults + smoke-seed spec. Buff-pill HUD + green "+40" popup. Full MVC module map. Formalize bonus pickup colors/glyphs + buff-pill + popup visuals.
13. 2026-06-05 — FROM artist TO writer — art_spec.md v2 added: 5 diamond pickups (26 px, r=13, white outline, dark-ink letter `+ F R S 2`) with final per-kind hex (Repair=#3CD25A · Fan=#FF8C28 · Rapid=#50DCFF · Shield=#B4DCFF · Score=#FFD246); 4 new colors. Buff pills = 14×14 box + 40×6 shrink bar at (12,36+18·i), stable order (Fan,Rapid,Shield,Score), Repair excluded. Green "+40" repair popup (528,30), 30 f. Shield blink + optional bubble ring. Name the 5 bonuses + HUD letters + repair-popup text.
14. 2026-06-05 — FROM writer TO level-designer — story.md v2 added: 5 names (REPAIR / FAN / RAPID / SHIELD / SCORE ×2); pill letters `+ F R S 2` confirmed; repair popup `+40` tied to REPAIR_HP; paste-ready config block. Own the v2 spawn numbers (drip cadence, enemy-drop %, kind weights, on-screen cap), balanced for 1–3 min runs; confirm the smoke seed makes a bonus reachable+expirable in 120 f.
15. 2026-06-05 — FROM level-designer TO programmer — level_spec.md v2 added: spawn economy LOCKED (CONFIRM GDD §V2.5) — drip `randint(600,840) f`; enemy-drop 15 % on bullet-kills only; kind weights R30/F20/Ra20/S15/Sc15; on-screen cap 3. AC13 holds (v1 ramp UNCHANGED). Smoke seed CONFIRMED (Rapid @(300,700), dur 60 → collect ~f3, expire ~f63). Implement the modular MVC refactor + bonuses per GDD §V2.9 + this economy.
16. 2026-06-05 — FROM programmer TO qa-tester — v2 built. workspace/game/ refactored into the GDD §V2.9 MVC package (~1205 lines/19 files, no cap, `-m game.main` works). All R23–R33 + R34 implemented. main.py --smoke-test exits 0 after exactly 120 headless frames AND runs the full Rapid lifecycle from the §V2.5 seed (collect f2 → cd6 → expire f61 → cd12), 3× stable. v1 R1–R18 + smoke gate intact. Verify AC14–AC21 (+ no AC1–AC13 regression) and write qa_report.md (FAIL → back to programmer).
17. 2026-06-05 — FROM qa-tester TO orchestrator — **PASS** (v2). Smoke gate green after the refactor (exit 0 / exactly 120 f / 3× + `-m` + compileall clean) AND lifecycle-exercising. AC14–AC21 all PASS (32 logic assertions + render probe); R34 Score×2 verified; R35 deferred. No v1 regression. Package modular (21 files/1222 lines, 36-line main.py, C3 retired). See qa/qa_report.md. Declare v2 DONE.
18. 2026-06-05 — FROM orchestrator TO human (project complete) — **DONE (v2 shipped).** "Starshard" + pickup bonuses on a modular MVC package. All v2 MUST R23–R33 verified + COULD R34 working; smoke gate green and lifecycle-exercising; no v1 regression. Play with `.\.venv\Scripts\python.exe workspace\game\main.py`. Two non-blocking items parked (AC13 long runs; R35 screen-clear) IF a human playtest confirms.

— — — v3 increment (knowledge-base reorganization) opened 2026-06-05 — — —

19. 2026-06-05 — FROM human TO manager — New role hired. Bring order to the chaotic workspace: split the large append-only docs into smaller scoped parts, create per-role sub-directories so each role reaches only what it needs without loading the whole project history, and realign the other roles to the new layout. Keep spec bodies intact (they match the shipped code).
20. 2026-06-05 — FROM manager TO human (reorg complete) — DONE. workspace/ scoped into per-role folders (requirements/ design/ art/ story/ levels/ qa/ + shared/ + archive/), specs relocated verbatim, growing narrative pulled out of the hot path into per-domain history.md + archive/. Added roles/manager.md, registered it in CLAUDE.md, and updated every role's "Read first" + the blackboard map (workspace/README.md). Specs unchanged → game still matches; nothing lost.

— — — v4 increment (QA documentation: feature inventory + checklists + test plans) opened 2026-06-05 — — —

21. 2026-06-05 — FROM orchestrator TO qa-tester — Standing QA docs requested by the human. Produce: (1) a feature inventory documenting every implemented feature (v1 + v2) mapped to its requirement IDs (R1–R35), ACs, and game module(s); (2) per-feature QA checklists (concrete pass/fail steps); (3) a smoke-test plan (the headless 120-frame gate) AND a regression-test plan (full re-verify across v1+v2 features). These are durable docs separate from the per-run qa_report.md. Don't change game code.
22. 2026-06-05 — FROM qa-tester TO orchestrator — **v4 QA docs delivered.** `qa/feature_inventory.md` maps **32 features (F1–F32)** across v1+v2 to their R#/AC#/module(s) (incl. the intentional gaps R35/R20/R22), and `qa/test_plan.md` holds the **smoke-test plan** (120-f gate, seeded Rapid spawn→collect→apply→expire lifecycle), the **regression-test plan** (full v1+v2 re-verify order + pass bars), and **per-feature checklists** with observable PASS steps. No game code changed; `qa_report.md` left as the per-run log. Smoke gate **re-run → exit 0** (confirms the documented expected result). Writing the checklists surfaced **no defects** (code matches specs) → no route-back to programmer. Declare v4 DONE.
23. 2026-06-05 — FROM orchestrator TO human (project complete) — **DONE (v4 shipped).** Standing QA docs are in place: `qa/feature_inventory.md` (32 features F1–F32 → R#/AC#/module) + `qa/test_plan.md` (smoke + regression plans + per-feature checklists), kept distinct from the per-run `qa_report.md`. No game-code change; smoke gate re-run → exit 0; checklist authoring surfaced no defects. Game still plays via `.\.venv\Scripts\python.exe workspace\game\main.py`.

— — — v5 increment (three enemy types: varied movement, fire & bullet behaviors) opened 2026-06-05 — — —

24. 2026-06-05 — FROM human TO orchestrator — Add **enemy variety**: keep the existing enemy but give its aim a **deviation** (fire at the player's general area, not dead-on); add a **heavy** enemy (bigger, slower) firing **green** medium-velocity pellets that **split midway** into **3 red fan bullets** preserving heading; add a **scout** enemy (faster) firing **high-velocity high-accuracy cyan** bullets.
25. 2026-06-05 — FROM orchestrator TO business-analyst — v5 kickoff. Captured the 3-enemy feature in `shared/brief.md` (v5 section) + opened the v5 backlog. Define requirements: regular enemy + aim-deviation tweak; heavy enemy + splitting-green→3-red-fan projectile; scout enemy + fast/accurate cyan. Spell out per-type ACs and the smoke-gate expectation (seed a split). Delegate exact numbers to the Designer/Level-designer. Append a dated v5 section to requirements.md.
26. 2026-06-05 — FROM business-analyst TO lead-game-designer — requirements.md v5 section added (§13–§17): R36–R44 MUST (typed REGULAR/HEAVY/SCOUT roster; regular + per-shot aim-deviation cone that collapses to today's exact aim at zero; heavy bigger/slower firing a GREEN splitting pellet; R39 split lifecycle = travel → midway split on a FIRE-TIME-frozen distance along its own heading → exactly 3 RED children fanned about that heading, then ordinary red bullets, no re-split/no homing; scout faster/fragile firing fast accurate CYAN; spawn-mix folds into ramp keeping AC13; smoke gate preserved + a split SEEDED headless; internal kind names). All numbers DELEGATED in the §15 Open-values table. AC22–AC29. Set the per-type numbers in the GDD: sizes, move speeds, HP, bullet speeds (red/green medium, cyan high), fire cadences, deviation cone half-angles (regular moderate > scout tight/0), split fraction (~midway), fan half-angle + center-child rule, child bullet speed.
27. 2026-06-05 — FROM lead-game-designer TO artist — GDD v5 section added (§V5.1–§V5.9): **every §15 lever LOCKED.** Roster (§V5.2): REGULAR (existing, r=13/2 HP/red 4.5/+50, **+ aim cone ±12°**), HEAVY (r=18, entry 1.4/strafe 1.2, 4 HP, +80, **green** pellet 4.5, fire ×1.6, cone ±6°), SCOUT (r=10, speed 3.0, 1 HP, +60, **cyan 7.5** high, fire ×1.4, cone ±3°). Aim = one routine, per-shot uniform(−cone,+cone) → **cone 0 == today's enemy** (regression-safe); cones monotone 12>6>3. Split (§V5.4): **FIRE-TIME-frozen** heading + `S=max(0.5·D,60)` as timer `round(S/4.5)` → 3 RED children at (−18°,0°,+18°) about heading (**fan ±18°, center INCLUDED, exactly 3**), child speed 4.5, terminal/no re-split; surviving pellet always splits. Bullet dmg/collision flat 15/r=5 for ALL enemy bullets (green only *drawn* bigger). Smoke (§V5.6): seed green pellet @~f3 → splits ~f16 (AC27). Ramp INTENT stated, weights left to level-designer (R41/AC13). Formalize: 3 distinguishable enemy body shapes/sizes (HEAVY big ~36×32 / REGULAR ~26×24 unchanged / SCOUT small ~20×18) + RED/GREEN/CYAN bullet hex (green drawn larger; keep distinct from magenta enemy, cyan player/Rapid, green Repair/HP).
28. 2026-06-05 — FROM artist TO writer — art_spec.md v5 section added (§V5.1–§V5.6). **3 enemy bodies, all magenta `ENEMY` fill, told apart by silhouette+size+outline weight:** HEAVY = armored octagon 36×32 (r=18, 3px edge), REGULAR = chevron 26×24 unchanged (r=13, 2px), SCOUT = sharp dart 20×18 (r=10, 1px) — paste-ready vertices for each. **3 bullet families by hue:** RED `#FF5A28` (reuse BULLET_E; regular + all split children), GREEN `#8CF03C` toxic-lime heavy pellet (drawn r=8 + 2px white core, collision still r=5), CYAN `#2DCDFF` electric-ice scout (drawn as a 12px streak + r=4 head). Anti-clash confirmed vs Repair/HP green, player cyan/bullet, magenta body (R42). Optional split white-ring burst specced + cuttable. Bless the internal kind names REGULAR/HEAVY/SCOUT (R44) + supply any UI labels for the new enemies/bullets.

29. 2026-06-05 — FROM writer TO level-designer — story.md v5 section added: **blessed** the canonical internal kind names `REGULAR` / `HEAVY` / `SCOUT` (R44) — final, match GDD §V5.2 + the `ENEMY_KINDS` keys, no synonyms; bullet families keep the Artist's RED/GREEN/CYAN labels. **UI copy decision: NONE needed** — no bestiary/enemy/bullet labels (R44 + non-goals), no per-kill score feed (HUD stays the single cumulative SCORE), and the v1 pitch line already covers all enemies; every v1/v2 string still applies verbatim, so the Programmer pastes nothing new from the Writer for v5. Own the v5 spawn mix/weighting: keep REGULAR from t=0, gate HEAVY/SCOUT per GDD §V5.7 intent, protect AC13 (1–3 min), re-confirm the §V5.6 smoke seed splits headlessly.

30. 2026-06-05 — FROM level-designer TO programmer — level_spec.md v5 section added (§V5.1–§V5.6). **Spawn mix LOCKED, folded into the UNCHANGED v1 spawner** (kind chosen at the instant `enemy_cap(t)`/`enemy_spawn_interval(t)` fire — no new path, no extra count): band weights **Warmup(0–20s) R100 · Heat-up(20–50) R85/H15 · Squeeze(50–90) R60/H15/Sc25 · Storm(90+) R55/H15/Sc30**, gates `HEAVY_GATE=20s` / `SCOUT_GATE=50s`. Deterministic `choose_enemy_kind(t,rng)` ladder supplied (mirrors v2 §V2.1). AC13 protected (variety = texture not volume; SCOUT 1-HP fragility self-limits its on-screen presence; SCOUT gate guards the low tail; the split/accuracy nudge the high tail down — v1 §3 ramp + v2 economy untouched). AC28 holds vs the §5 run-length model. **AC27 CONFIRMED:** smoke spans t=0–2s = Warmup = REGULAR-only, so the §V5.6 seeded green pellet is the sole clean split — weighting leaves it intact. v2 enemy-drop (15 %/bullet-kill) unchanged, applies to all kinds. Implement enemy-kind selection per §V5.1 + the GDD §V5.2–§V5.6 stats/split/seed.

31. 2026-06-05 — FROM programmer TO qa-tester — **v5 built.** `config.py` gains the §V5.5 `ENEMY_KINDS` table (REGULAR/HEAVY/SCOUT: r/entry/strafe/descent/hp/score/ram/bullet/bspeed/fire_mult/cone), the split constants (`SPLIT_FRACTION=0.5`, `SPLIT_MIN_DIST=60`, `FAN_HALF_ANGLE=18`, `CHILD_SPEED=4.5`), RED/GREEN/CYAN bullet hex (art_spec §V5.2 final), and `choose_enemy_kind(t,rng)` (level_spec §V5.1 ladder, gates 20/50). One aim routine with a per-shot `uniform(−cone,+cone)` perturbation → cone 0 reduces bit-for-bit to v1 exact aim (AC29); REGULAR ±12 / HEAVY ±6 / SCOUT ±3 (verified monotone). HEAVY's GREEN pellet freezes `u` + `S=max(0.5·D,60)` at fire time → `split_timer=round(S/4.5)` → bursts into **exactly 3 RED children** at (−18°,0°,+18°) about the frozen heading (speed 4.5, terminal, never re-split); hit-player/off-screen-before-split → no children. Kind folds into the UNCHANGED v1 spawner (replaces a REGULAR slot, never adds to `enemy_cap`); enemy-drop 15%/bullet-kill applies to all kinds. Per-kind body shapes (HEAVY octagon 36×32 / REGULAR chevron / SCOUT dart) + 3 bullet draws (red dot / r=8 green pellet+core / cyan streak), collision flat r=5 for all. `--smoke-test` exits 0 after exactly 120 f AND seeds a green pellet @f3 that splits ~f15→3 children update to f120 (AC27); 3× stable, `-m game.main` + compileall clean; v1/v2 (incl. the seeded-Rapid lifecycle) intact. Verify AC22–AC29 (+ no AC1–AC21 regression) and write qa_report.md (FAIL → back to programmer).

32. 2026-06-05 — FROM qa-tester TO orchestrator — **PASS (v5).** Smoke gate green (exit 0 / exactly 120 f / 3× + `-m game.main` + compileall clean) AND split-lifecycle-exercising: the §V5.6-seeded GREEN pellet lives frames 3–14, bursts ~f15 into exactly 3 RED children that update to f120 (AC27). AC22–AC29 all PASS (19-assertion AC harness + AC27 real-loop split-lifecycle probe + 6-assertion v1/v2 no-regression probe): three distinct kinds (H18>R13>S10, entry 1.4<2.0<3.0); REGULAR red per-shot spread spans full ±12° (sd 6.99°); HEAVY green medium 4.5 pellet; split→exactly 3 RED at (−18°,0°,+18°) about a **fire-time-frozen** heading (proven non-homing + edge cases); SCOUT cyan 7.5 ±3° (cones monotone 12>6>3); cone-0 REGULAR aims bit-for-bit dead-on (AC29); spawn gates HEAVY@20s/SCOUT@50s, all three in Storm, kind chosen inside the unchanged v1 spawner (AC13 untouched); no external assets. No v1 (AC1–AC13) or v2 (AC14–AC21) regression. See qa/qa_report.md. Declare v5 DONE.

33. 2026-06-05 — FROM orchestrator TO human (project complete) — **DONE (v5 shipped).** "Starshard" now fields a **three-enemy roster** on the modular MVC package: REGULAR (existing, now firing through an aim cone whose zero case is bit-for-bit the v1 dead-on shot), HEAVY (bigger/slower, GREEN pellet that freezes its heading at fire time and bursts at the midway point into exactly 3 RED fan children — terminal, no homing/re-split), and SCOUT (fast/fragile, accurate CYAN). All v5 MUST R36–R44 verified + AC22–AC29 PASS; smoke gate green (exit 0 / exactly 120 f / 3× + `-m game.main` + compileall clean) and split-lifecycle-exercising; no v1 (AC1–AC13) / v2 (AC14–AC21) regression. Play with `.\.venv\Scripts\python.exe workspace\game\main.py`. **Carried, still-non-blocking:** the AC13 long-run caveat (expert pure-dodging can exceed 3 min); R35 screen-clear bonus also still deferred.

## 2026-06-05 (v6) — Verdict: ✅ **PASS** (ship v6 — bombs / panic button + Z-fire / X-bomb remap)

**Bottom line.** All v6 MUST requirements **R45–R54 verified** and acceptance criteria **AC30–AC38
PASS**. The bomb is a charge-gated screen flush: pressing **X** with **charges > 0** spends exactly one
charge, **bulk-clears the three hostile lists** (all enemies + all asteroids/debris + **all** enemy
bullets — incl. in-flight GREEN split pellets and already-split RED children), arms the **18-frame
near-white flash** and an **18-frame lockout**, and **spares** player bullets + all six pickups +
cosmetics — awarding **no score** (silent despawn). At **0 charges X is a complete no-op** (no flush, no
flash, no decrement). The new **6th `BonusKind.BOMB`** pickup grants **+1 charge clamped to the cap (4)**
with **no buff-pill** (collect-at-full = WASTED), and is the **rarest** kind (weight **6/100**, both spawn
paths sharing the re-sliced ladder). Controls are remapped to **Z = fire / X = bomb** with the
start-screen copy rewritten (no stale "Space" text); Z still honors the cooldown + v2 Rapid/Fan modifiers.
The headless smoke gate **exits 0 after exactly 120 frames (3× stable, `-m game.main` + compileall clean)**
*and* **exercises a real bomb activation** at ~f20 that clears the v5 split children (charge 2→1, flash
armed). **No regression** in v1 (AC1–AC13), v2 (AC14–AC21), or v5 (AC22–AC29). Per the handoff rule this is
a **PASS → orchestrator (declare v6 DONE)**.

### How it was tested (transparency)
A live human-driven window isn't observable from this headless QA environment, so v6 behaviour was
verified by **driving the real game logic/systems directly** (the standing practice; see `test_plan.md §0`):
- `game/main.py --smoke-test` run as shipped (the required gate) — **3×** + via `-m game.main` (from
  `workspace/`) + `-m compileall`.
- A **v6 AC harness** drove the **real `App` smoke loop** (faithfully mirroring `app.run`'s seeds: Rapid
  @f2, GREEN split pellet @f3, scripted **X @f20**) with `bombs._flush` instrumented, and drove the real
  `World`/`bombs`/`buffs`/`combat`/`physics` for the remaining ACs — **18 assertions, all green**. It
  proved the **f20 flush "before" snapshot held 1 enemy + 3 asteroids + 3 RED split children** and the
  "after" snapshot was `(0,0,0)` while **player bullets were untouched (3→3)** and the flash armed to 18.
- A **no-regression probe** re-ran v1 (bullet-kill + score, asteroid contact damage + i-frames), v2 (Rapid
  collect→cd 6, Repair clamp-no-overheal, restart cleanup), and v5 (GREEN pellet → exactly 3 RED children
  at the (−18°,0°,+18°) fan) against the package with the bomb system live — all green.

(The scratch harness was removed after the run; commands + results are reproduced below.)

### Commands run
```
$env:SDL_VIDEODRIVER="dummy"; $env:SDL_AUDIODRIVER="dummy"
.\.venv\Scripts\python.exe workspace\game\main.py --smoke-test      # → exit 0  (run 3×: 0/0/0)
cd workspace ; ..\.venv\Scripts\python.exe -m game.main --smoke-test # → exit 0  (package importable from pkg root)
.\.venv\Scripts\python.exe -m compileall -q workspace\game          # → exit 0  (no syntax/import errors)
```
Environment: Python 3.14.3 · pygame-ce 2.5.7 (SDL 2.32.10) · Windows 11.

### Smoke-test gate (AC37) — preserved + bomb activation exercised
```
pygame-ce 2.5.7 (SDL 2.32.10, Python 3.14.3)
smoke run 1 exit: 0 | run 2 exit: 0 | run 3 exit: 0 | -m game.main exit: 0 | compileall exit: 0
AC37 probe: split RED children present f15–19 (max 3 ebullets) → scripted X @f20 →
            flush before=(enemies 1, asteroids 3, ebullets 3) → after=(0, 0, 0);
            charge 2→1; flash armed to 18; player bullets 3→3 (spared).  → PASS
```
Exits 0 after exactly 120 headless frames. The §V5.6 GREEN pellet (seeded @f3, forced short split) bursts
~f16 into 3 RED children (AC27 still observed), which are then **cleared by the scripted v6 bomb @f20**
along with the seeded enemy + remaining asteroids — proving the flush reaches the v5 split children. The
v2 seeded-Rapid lifecycle (different system) is unaffected. **PASS.**

### v6 acceptance-criteria checklist

| AC | Area | Result | Evidence |
|----|------|--------|----------|
| **AC30** | X (charges>0) → −1 charge + flush + flash | ✅ PASS | one X edge with charges>0 and lockout==0 → `charges 2→1`, `flash_timer=18` (FLASH_FRAMES), the three hostile lists cleared in the same call (`systems/bombs.update`). HUD reads the live `world.charges` so the readout drops by 1 by construction. |
| **AC31** | 0-charge X = complete no-op | ✅ PASS | with `charges=0`, an X edge leaves `charges=0`, `flash_timer=0`, `bomb_lockout=0`, and all hostile lists untouched — the `and world.charges > 0` guard short-circuits the whole activation (no flush/flash/decrement). |
| **AC32** | Flush list scope; spares pickups + player bullets | ✅ PASS | `_flush` clears `world.enemies` + `world.asteroids` + `world.ebullets` (RED/GREEN/CYAN incl. an in-flight GREEN pellet **and** already-split RED children) → all 0; `world.pbullets` and `world.bonuses` (a BOMB pickup + a REPAIR pickup) **survive untouched**. Confirmed both in isolation and in the real smoke loop (3 RED children + enemy + asteroids cleared; 3 player bullets kept). |
| **AC33** | Brief full-screen flash that fades; no flash on no-op | ✅ PASS | a successful activation sets `flash_timer=18`; `hud.draw_flash` blits a `FLASH_TINT #F0F8FF` overlay at `alpha = 200·flash_timer/18` (peak 200 on the activation frame → linear fade → gone at f=18), PLAY-only, above the HUD. The 0-charge no-op (AC31) never sets `flash_timer` → **no flash**. |
| **AC34** | Pickup +1 clamped; collect-at-full WASTED; scarcer | ✅ PASS | `buffs.apply(BOMB)` does `charges = min(BOMB_CAP, charges+1)`: 1→2 normally; at cap 4 it **stays 4** (consumed/WASTED, never exceeds cap); BOMB stores **no `buff_timers` entry** (no pill). Weight ladder re-slice = Repair30/Fan20/Rapid20/Shield12/Score12/**BOMB6** (sums 100) → BOMB is **strictly the rarest** kind, both spawn paths sharing it. |
| **AC35** | Z fires (cooldown + Rapid/Fan intact) / X bombs; copy updated | ✅ PASS | `input.read_input` fires on **`K_z`** (Space kept only as a silent alias); X is an edge `KEYDOWN K_x` in `app._handle_events` (`bomb_fired=True`); movement/restart(`R`)/quit(`Esc`) unchanged. A Z shot is a **single beam, cd 12**; with Rapid up **cd 6**; with Fan up **3 beams** — cooldown + v2 modifiers intact. `CONTROLS_1 = "MOVE  Arrows / WASD      Z = fire · X = bomb"` — **no stale "Space" text**. |
| **AC36** | One-press-one-bomb; restart resets to 2, no leak | ✅ PASS | holding X True for 10 consecutive frames drains **exactly one** charge (the 18-f `bomb_lockout` + key-down edge gate block repeats). `reset_run()` sets `charges=BOMB_START=2` and zeroes `flash_timer`/`bomb_lockout`/`bomb_popup_timer` → no bomb/flash state leaks into the new run. |
| **AC37** | Smoke 120 f exit 0 + bomb activation exercised | ✅ PASS | exit 0 after exactly 120 frames headless, 3× + `-m` + compileall; the real smoke loop fires the scripted X @f20 (`_flush` called **exactly once**), clearing the live hostile lists incl. the 3 v5 split children, charge 2→1, flash armed — without raising. |
| **AC38** | Reuses entity lists + v2 pickup path; clamped int; shapes-only | ✅ PASS | the flush operates on the **existing** `enemies`/`asteroids`/`ebullets` lists; the bomb pickup is the 6th `BonusKind` flowing through the **v2 collect→`buffs.apply`** path; `world.charges` is an explicit `int` clamped `[0, BOMB_CAP]` (guarded at 0, `min()` at the cap); the flash + HUD readout are `pygame.draw`/`Surface` overlays only — **no external image/sound/font assets** anywhere in `game/`. |

### v6 MUST → AC traceability (all met)
R45 charge pool (start 2 / cap 4 / clamp / restart→2) → AC30/AC34/AC36/AC38 · R46 X-activates (edge, −1,
flush+flash) → AC30 · R47 0-charge no-op → AC31 · R48 flush scope (enemies+asteroids+all enemy bullets;
spare player bullets+pickups+cosmetics; no score) → AC32 · R49 "on-screen-now", spawning resumes → AC32 (+
smoke repopulates) · R50 full-screen flash, fades, 1:1 with a successful activation → AC33 · R51 6th
`BonusKind.BOMB` = +1 clamped, instant/no-pill, WASTED-at-full, sole refill → AC34 · R52 Z fire / X bomb +
copy update → AC35 · R53 one-press-one-bomb (edge + lockout) → AC36 · R54 smoke gate + seeded bomb
activation → AC37. **COULD R55** post-bomb lull = `BOMB_SPAWN_LULL=0` (intentionally no lull) — non-failing.

### Regression check (no v1/v2/v5 regression)
Re-ran against the package with the bomb system live: **v1** — a player bullet destroys a small asteroid
(+10) and asteroid contact deals −20 HP + 60-f i-frames. **v2** — the Rapid pickup collects (cd 12→6),
Repair from 80 clamps to 100 (no overheal), and `reset_run()` clears buffs/bonuses while preserving session
`best` **and** resets `charges` to 2. **v5** — a GREEN pellet matures into **exactly 3 RED children** at the
(−18°, 0°, +18°) fan about its frozen heading. All green. The smoke gate (AC1/AC2/AC20/AC27) is the same
gate that exits 0 above. The **AC13 long-run caveat** is **unchanged and still non-blocking** — the
level-designer folded the bomb economy into the QA-passed v1 ramp by a **volume-neutral re-slice** (Shield
15→12, Score 15→12; drip cadence / enemy-drop % / on-screen cap bit-for-bit v2), and the bomb is a survival
tool (no score), so it does not lengthen runs. No new playtest evidence either way.

### Notes / observations (no action required)
- **Ordering is the "I bombed in time" guarantee:** `bombs.update` runs in `_step_play` **before**
  `combat.resolve`'s §V2.7 player-damage step, so a bomb on the contact frame clears the bullet *before* it
  can deal damage (verified: an enemy bullet on the player + a same-frame bomb → **0 HP lost**).
- **The flush is a pure list clear** (`enemies.clear()` / `asteroids.clear()` / `ebullets.clear()`),
  reusing the existing lists with no special despawn path and no scoring call — so green pellets and red
  children fall out automatically (they are ordinary `ebullets`), and "no score" is structural, not a flag.
- **`bomb_lockout` is belt-and-suspenders** over the key-down edge: even if the edge were mis-fed as a held
  level, the 18-f lockout (== `FLASH_FRAMES`) prevents a second activation mid-flash, so two white-outs
  can't strobe and a single press can't double-drain.
- **HUD readout is correct by construction** — it renders `f"×{world.charges}"` straight off the live
  integer (violet sphere icon, dimmed/hollow at 0), so −1/+1/→2 transitions need no separate bookkeeping.

**Final verdict (v6): PASS.** R45–R54 verified; AC30–AC38 all PASS; smoke gate green and bomb-activation
exercising; no v1/v2/v5 regression. → hand off to **orchestrator** to declare v6 DONE.

---


# QA report — v18 (Fan side-beam 2:1 nerf + Rapid → Overdrive/Railgun; bullet speed buffable)

Date: 2026-06-07 · QA · Verdict: **PASS** · Scope: verify the v18 bonus rebalance — (1) Fan side beams fire
exactly half the center cadence (2:1) and Fan is rarer; (2) Rapid is retired everywhere; (3) Overdrive (`O`)
and Railgun (`V`) apply/refresh/expire→baseline & restart-reset with bullet speed now a buffable stat;
(4) strongest-wins per stat (min cd / max speed) is bounded + clean-reverting; (5) the RNG ladder still sums
100 non-overlapping; (6) the smoke seed exercises a new kind — with **no AC1–AC93 regression**.

Verified against `requirements/v18.md` (R106–R113, AC94–AC101), `design/gdd/v18.md` (§V18.3/§V18.4),
`levels/level_spec/v18.md` (magnitudes + ladder + 2:1 contract), `story/v18.md` (O/V), `art/art_spec/v18.md`
(colors/pills), and the live `game/` package (`config` v18 block, `world.BonusKind`, `entities/player`
fire_cooldown/bullet_speed, `entities/projectiles.make_player_shots`, `systems/physics` firing, `systems/buffs`).

## Gates (both hard gates green — test_plan §2.5)
- **Full pytest suite (BLOCKING — green AND ≥75):** `pytest workspace\tests` → **106 passed / 0 failed**.
  Suite grew **94 → 106 (+12)**: +11 Programmer unit (`tests/unit/test_v18_bonus_rebalance.py`, R106–R112)
  and +1 QA e2e (`test_app_lifecycle.py::test_ac101_smoke_overdrive_lifecycle`). ≥75 parity floor holds. ✅
- **Smoke gate (BLOCKING):** `main.py --smoke-test` → exit **0** (3× stable); `compileall -q workspace\game`
  → exit **0**, no warnings. `-m game.main --smoke-test` → exit 0 **with `PYTHONPATH=workspace`/from `workspace\`**
  (the bare-from-root form can't resolve `game` since the package lives under `workspace/` — a pre-existing
  path quirk, not a v18 change; the in-process `App(smoke=True).run()` e2e covers the package-entry path). ✅

## The v18 behaviours — verdicts (AC94–AC101)

| AC | Behaviour | Verdict | Evidence |
|---|---|---|---|
| **AC94** | Fan center fires at cadence; sides (±12°) fire half as often (2:1) | **PASS** | Drove `physics.update_play` with fire held over 1200 f under Fan: **center=100 volleys, side=50** (exactly 2:1); every volley is **1 (center-only) or 3 (full fan)** beams — never 2/6. Fresh Fan resets parity → first volley is the full fan. Negative: side<center (sides genuinely skip). `test_r106_*` + probe §2 |
| **AC95** | Fan rarer than its old weight 20 | **PASS** | Ladder Fan width **12** (< 20); strictly rarer. `test_r107_*` + probe §3 |
| **AC96** | Rapid gone everywhere | **PASS** | No `BonusKind.RAPID`, no `C.RAPID_CD`; absent from `BONUS_LETTERS/COLORS/NAMES/BUFF_DURATION/TIMED_ORDER`; `pick_bonus_kind` never yields `"RAPID"` across all 0–99 rolls **and** a 20 000-draw sample; `SMOKE_BONUS_KIND="OVERDRIVE"`. `test_r108_*` + probe §3/§6 |
| **AC97** | Overdrive: cd halved (12→6) + speed up a bit; reverts | **PASS** | On collect `fire_cooldown==6` and `bullet_speed==12`; on expiry both revert to 12/10. `test_r109a_*` + probe §1/§4 |
| **AC98** | Railgun: speed up a lot (10→16) + cd up a bit (12→9, smaller cut); reverts | **PASS** | On collect `bullet_speed==16`, `fire_cooldown==9`; `6<9<12`, cut (3) < Overdrive's (6); reverts to 10/12. `test_r109b_*` + probe §4 |
| **AC99** | New kinds' combined weight == 20; ladder sums 100, non-overlapping, 7 kinds | **PASS** | Independently mapping every roll 0–99: `{REPAIR 30, FAN 12, OVERDRIVE 10, RAILGUN 10, SHIELD 12, SCORE 20, BOMB 6}` → sum **100**, every roll → exactly one kind (exhaustive+exclusive), OD+RG=**20**, **7** kinds. `test_r111_*` + probe §3 |
| **AC100** | Both timed buffs (pill + countdown); refresh-no-multiply; expiry→baseline; restart clears; cross-stat deterministic/bounded/clean-revert | **PASS** | Both active → cd `min(12,6,9)=6` (OD wins) / speed `max(10,12,16)=16` (RG wins) — **not composed** (cd never <6). Expire OD → cd 9/speed 16; expire RG → 12/10 (stepwise clean drop). Re-collect refreshes to full duration, effect unchanged. `reset_run` → `buff_timers={}`, cd 12, speed 10, fan parity 0. 5-pill stack (FAN/OVERDRIVE/RAILGUN/SHIELD/SCORE) renders in `test_render_smoke`. `test_r112_*` + probe §4/§5 |
| **AC101** | `--smoke-test` exits 0 / 120 f with the repointed seed exercising a full lifecycle; suite green & grown; no AC1–AC93 regression | **PASS** | Instrumented the **real** `App(smoke=True).run()`: Overdrive **applied frames 2–60** (cd 6 + speed 12), **reverted to baseline** before frame 120; exits 0 at exactly 120 f. Suite 94→106. `test_ac101_smoke_overdrive_lifecycle` + probe §1 |

## R110 watch-items (bullet-speed buffable) — verified
- **No tunneling at 16 px/f:** `RAILGUN_SPEED 16 < 2*AST_S_R = 24` (smallest target diameter) → a Railgun
  bullet cannot skip a target between frames. Larger targets never at risk.
- **Culling intact:** a 16 px/f bullet seeded near the top still despawns off the top within a few frames
  (off-screen filter unchanged). Collision path unchanged (bullets carry vx/vy; only magnitude grows).

## No AC1–AC93 regression
- Full suite **106/106** green: all v1–v17 unit + e2e lanes pass (combat/buffs/bombs/save/strings/physics/
  world/enemies/boss/nova/imports + e2e lifecycle/pause-quit/restart/layout/stats/render-smoke).
- v18 only re-slices the kind table (volume-neutral: drip cadence/drop %/on-screen cap bit-for-bit v2) and
  adds two buffable stats; the v1 ramp + v2/v5/v6 economy + the bonus framework (R23–R35) are untouched.
- Fan **center beam** still fires at the resolved cadence (full forward DPS preserved); geometry/angles/
  colors unchanged — only side-beam *cadence* + Fan's *weight* changed, per the non-goals.

## Commands run (headless: `SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy`, `STARSHARD_SAVE_PATH`→tmp)
- `python -m pytest workspace\tests` → **106 passed / 0 failed**.
- `python workspace\game\main.py --smoke-test` → exit **0** (3× stable).
- `python -m compileall -q workspace\game` → exit **0**.
- QA independent probe (real-smoke Overdrive lifecycle, Fan 2:1 over 1200 f, ladder 0–99 exhaustive map +
  20k-draw sample, strongest-wins stepwise revert, restart reset, RAPID-retired, tunnel/cull bound) → **all
  PASS**. Built as scratch, run, then **folded the strongest check into a permanent e2e test** (not committed
  as scratch — deleted per §1.5).
- `ruff check` on the new e2e test → clean (hygiene, non-blocking).

## Verdict
**PASS.** Fan side beams fire exactly 2:1 to center and Fan is rarer (12); Rapid is fully retired (no kind,
const, registry, ladder slot, pill, or smoke ref); Overdrive (cd 6 + speed 12) and Railgun (speed 16 + cd 9)
apply, refresh-without-multiply, expire to baseline, and reset on restart; cross-stat resolution is
strongest-wins (min cd / max speed), bounded (cd ≥ 6 > 0) and clean-reverting; the RNG ladder sums 100 over 7
non-overlapping kinds; the repointed smoke seed exercises a full Overdrive lifecycle headlessly. Both hard
gates green (smoke exit 0 + pytest **106/106**); no AC1–AC93 regression. → orchestrator (declare v18 DONE).

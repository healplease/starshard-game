# QA report — v16 (second boss NOVA + uniform-random, extensible boss pool)

Date: 2026-06-07 · QA · Verdict: **PASS** · Scope: verify the second boss (NOVA) appears, the boss pick is
uniform-random over an extensible pool, NOVA spawns **no ships** and is **deadlier** than the Mothership on
every axis, the Mothership keeps its v7 behaviour through the new selection layer, and there is **no
AC1–AC85 regression**. Full pytest suite + smoke gate.

Verified against `requirements/requirements/v16.md` (R99–R105, AC86–AC93), `design/gdd/v16-second-boss.md`,
`levels/level_spec/v16-second-boss.md`, `art/art_spec/v16-second-boss.md`, `story/story/v16-second-boss.md`,
the live `game/` package (config pool/specs/`pick_boss_type`, entities/boss, systems/encounter+combat,
view/render+hud), and `tests/unit/test_nova.py`.

## Gates (both hard gates green — test_plan §2.5)
- **Full pytest suite (BLOCKING — green AND ≥75):** `pytest workspace\tests` → **91 passed in 2.40 s,
  0 failed**. Suite grew **75 → 91 (+16)** = the new `tests/unit/test_nova.py`; the ≥75 parity floor holds. ✅
- **Smoke gate (BLOCKING):** `main.py --smoke-test` → exit **0**; `-m game.main --smoke-test` → exit **0**
  (package entry point, `PYTHONPATH=workspace`); `compileall -q workspace\game` → exit **0**, no warnings. ✅
  The forced seed `SMOKE_BOSS_TYPE="NOVA"` drives NOVA headlessly (arrival clear → entrance → ≥1 attack step).

## AC86–AC93 — verdicts

| AC | What | Verdict | Evidence |
|---|---|---|---|
| **AC86** | Extensible pool; boss #3 = one entry, N from length | **PASS** | `config.BOSS_POOL=("MOTHERSHIP","NOVA")` + `BOSS_SPECS` dict; `pick_boss_type` reads `len(BOSS_POOL)`; `test_pool_shape_and_registry` + `test_extensible_one_entry_is_length_driven` (append `PHANTOM` → ~1/3 each, zero rule edits) |
| **AC87** | Uniform i.i.d. seedable selection | **PASS** | `test_selection_uniform_and_seedable` (4000 draws ~2000±300 each; same seed → identical seq). Independent probe through the **real `encounter.update` trigger** over 3000 seeds → **MOTHERSHIP 1483 / NOVA 1517** (≈½/½) |
| **AC88** | Cadence unchanged (75 s, +90 s) | **PASS** | `BOSS_FIRST_MARK`/`BOSS_INTERVAL` untouched; selection rides the existing breakpoint (`encounter.update` picks the spec, then `boss_next_mark += BOSS_INTERVAL` exactly as v7); level_spec §V16.2 CONFIRM |
| **AC89** | Full v7 framing for whichever boss | **PASS** | One shared ARRIVAL→ENTRANCE→ACTIVE→DEFEAT loop in `encounter.py`; per-boss values read from spec/instance. Mothership parity suite `test_boss.py` (AC39–AC52) untouched & green |
| **AC90** | NOVA clearly distinct | **PASS** | Distinct silhouette via `_BOSS_DRAW[boss.type]` (blue pulsar disc+12 spikes vs magenta carrier), azure `NOVA_BULLET` render branch, name `NOVA` / blue bar+banner — `test_nova_strings` |
| **AC91** | NOVA spawns ZERO ships | **PASS** | NOVA moveset is bullets-only (no `_spawn_wave`/`make_enemy` path); `test_nova_full_cycle_spawns_no_ships`. Independent probe: forced-NOVA fight 400 f (~4 cycles) → **max enemies on screen = 0** |
| **AC92** | Deadlier on recorded levers, still winnable | **PASS** | `test_nova_deadlier_numbers`: bullet **25 > 15**, ram **80 > 60**, speed **5.5/6.0 > 4.5**, cadence **90 < 150 f**, reward **1500 > 1000**, HP **120 = 120** (deadlier via attacks, not sponge). Densities/aim per GDD §V16.5; winnable caveat in level_spec §V16.4 |
| **AC93** | Smoke 0 + suite grown + Mothership parity + no AC1–AC85 regression | **PASS** | 91/91 green, smoke exit 0; suite grew +16; `test_boss.py` (AC39–AC52) + all v1–v15 e2e/unit lanes green; `test_bare_boss_is_mothership_backcompat` confirms the routing layer left Mothership defaults intact |

## Hard human constraints (non-negotiable gates)
- **R103 no ship spawning:** PASS — verified both in unit (full-cycle) and an independent 400-frame forced
  fight (zero enemies). NOVA's only hostiles are its own bullets; the v1/v5/v6 spawners stay frozen (a
  *stricter* freeze than the Mothership, which adds minions).
- **R104 deadlier attacks:** PASS — every lethality axis is strictly worse than the Mothership, headline
  per-hit damage **25 > 15**, all values recorded in GDD/level_spec; routed through the existing
  enemy-bullet × player path (`test_nova_bullet_damage_to_player` = 25, `test_nova_ram_damage` = 80).

## Commands run (headless: `SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy`, `STARSHARD_SAVE_PATH`→tmp)
- `python -m pytest workspace\tests` → **91 passed / 0 failed**.
- `python workspace\game\main.py --smoke-test` → exit **0**; `-m game.main --smoke-test` → exit **0**.
- `python -m compileall -q workspace\game` → exit **0**.
- Independent QA probe (selection-via-encounter ≈½/½ over 3000 seeds; forced-NOVA 400-f fight → 0 ships) →
  `PROBE OK`. Built as scratch, run, deleted (not committed) per test_plan §1.5.

## Notes (non-blocking)
- Ruff/pyright are the Programmer's pre-handoff hygiene (§2.5, non-blocking); not re-run here — the two
  hard gates (smoke 0 + pytest green) are both met.
- Selection randomness is never exercised by the natural TIME gate in-budget (mark ≫ 120 f); it is covered
  by the seedable unit tests + the QA probe through the real trigger path.

## Verdict
**PASS.** NOVA appears and is distinct; the pick is uniform i.i.d. over an extensible, length-driven pool
(seedable/forceable); NOVA spawns zero ships and is measurably deadlier than the Mothership on every axis
while staying winnable; the Mothership is unchanged through the selection layer; full suite **91/91** green
and both smoke invocations + compileall exit 0 with no AC1–AC85 regression. → orchestrator (declare v16 DONE).

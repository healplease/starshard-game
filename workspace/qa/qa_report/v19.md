# v19 — precise controls (Focus ×0.5 + always-on small circular hitbox + ~1.5× bullets)

## 2026-06-07 (v19) — Verdict: ✅ PASS

Owner: qa-tester · Scope: a new mechanic + balance change → **full rigor** (independent probe per feature
+ a negative test). Verified against `requirements/requirements/v19.md` (R114–R119, AC102–AC108),
`design/gdd/v19.md`, `levels/level_spec/v19.md`, `art/art_spec/v19.md`, `story/story/v19.md`, and the live
`game/` code.

### What I ran (all from `.venv`, headless dummy drivers)
- **Smoke gate** `workspace\game\main.py --smoke-test` → **exit 0**, 3× stable (no traceback/window/hang).
- **Package entry** `-m game.main --smoke-test` (cwd=workspace) → **exit 0**; `compileall workspace\game` → exit 0.
- **Full pytest suite** `pytest workspace\tests` → **117 passed** (≥75 parity floor; v18 106 → +11: 10 unit
  R114–R118 in `tests/unit/test_v19_precise_controls.py` + 1 e2e R117 in `tests/e2e/test_render_smoke.py`).
- **Independent QA probe** (scratch, driving the real input/physics/combat/render paths; deleted after) —
  **39/39 checks passed**, including the AC104 negative test across all four damage paths.

### AC checklist (AC102–AC108)
- **AC102 [Play/Code] PASS** — Hold-SHIFT halves the per-frame step on **both** axes (dx 5→2.5, dy 5→2.5);
  **releasing restores full 5.0 the same frame** (`world.focus` set/cleared per frame, held-not-toggle).
  Focused fire = 1 shot at baseline `FIRE_CD` (firing untouched). Focus is consumed only in the PLAY-only
  `physics.update_play`; **combat collision never reads focus**.
- **AC103 [Code] PASS** — `P_HITBOX_R == 6`, = 0.46·`P_R`(13) (≈50%); used by every player-damage test; the
  **drawn ship is unchanged** (`_ship_pts` still tip 15 / 14-wide). Always-on regardless of SHIFT.
- **AC104 [Play/Code] PASS (negative test)** — For ebullet/asteroid/enemy-ram/boss-ram, a hazard placed at a
  distance that **WOULD have hit under the old `P_R`** (control `_hit(P_R,…)`==True) but lies outside the new
  small circle now deals **0 damage**; a hazard inside `P_HITBOX_R` still deals full damage (15/20/40/60).
  Pickup collection **stays generous at `P_R`** (collected at a reach that the damage hitbox now misses).
- **AC105 [Code] PASS** — `EB_R 5→8`; **every** enemy/boss family draws at `EB_R` (measured outer ≈8 px for
  RED/GREEN/YELLOW/NOVA + CYAN head) → draw==collision; player bullet **6×18** (measured half-spans 3/9),
  collision `r=PB_H/2=9`; `CYAN_TAIL_LEN 18`; the retired draw consts (`PELLET_DRAW_R`,
  `NOVA_BULLET_DRAW_R`, `CYAN_HEAD_R`) are gone.
- **AC106 [Play] PASS** — With SHIFT held in PLAY a red disc (`#FF2840` @ alpha 128, center reads (128,20,32))
  draws on the ship at the **true** radius (measured outer = 6 = `P_HITBOX_R`); **absent** when SHIFT is up.
  Toggling focus leaves collision **identical** (dmg 15 both ways → render-only).
- **AC107 [Code] PASS** — Body radii unchanged: `EN_R 13`, asteroids 12/26, `BOSS_R 70`, `NOVA_R 60`,
  `ENEMY_KINDS` 13/18/10.
- **AC108 [Smoke] PASS** — `--smoke-test` exits 0 after 120 frames; `smoke_input` holds Focus over frames
  60–89 (f70 focused → step halved to 2.5; f40 unfocused) so the headless path exercises ×0.5 + the SHIFT
  indicator; the suite is green and grew by 11.

### Extra verification
- **No tunneling** (level_spec §V19.4): worst closing speed 12.5 px/f < `2·(P_HITBOX_R+EB_R)` = 28 — confirmed
  against live constants. No mis-cull (radii grew ≤3 px; center-based cull bounds unchanged).
- **Copy (R114):** `CONTROLS_1` is the locked string verbatim and renders **462 px** (FONT_SMALL) — fits the
  600 px window centered.
- **Lint/types (non-blocking):** unchanged from v18 — one pre-existing residual (`BONUS_WEIGHTS` ladder
  comment line length + `bonus.py` `BonusKind` forward-ref); no new v19 residual; pyright clean on changed files.

### Regression — no AC1–AC101 break
Full suite 117/117 green (every prior unit + e2e check) and the smoke lifecycle (seeded Overdrive
spawn→collect→apply→expire, split, bomb, boss) still completes inside 120 f. v1 ramp / economy / bullet
speeds / enemy bodies untouched (AC107 + constants). The only behavioral changes are the three v19 levers.

**Verdict: PASS — v19 ready to ship. Hand to orchestrator.**

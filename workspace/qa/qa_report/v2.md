## 2026-06-05 (v2) — Verdict: ✅ **PASS** (ship v2 — bonuses + modular refactor)

**Bottom line.** All v2 MUST requirements **R23–R33 verified**, plus the COULD extra **R34 (Score×2)**.
The headless smoke gate **survives the modular refactor** (exit 0, exactly 120 frames, no hang, 3×
stable) **and now exercises a full bonus lifecycle** (spawn→collect→apply→expire) inside the run.
**No regression** in v1 — AC1–AC13 re-confirmed against the new package. Per the handoff rule (MUST
fail → programmer; else → orchestrator), this is a **PASS → orchestrator (declare v2 DONE)**.

### How it was tested (transparency)
A live human-driven window isn't observable from this headless QA environment, so v2 behaviour was
verified by **driving the game's real logic and view code directly** (the same approach as the v1 report):
- `game/main.py --smoke-test` run as shipped (the required gate) — 3× + via `-m game.main`.
- A **behavioural harness** imported the real systems (`buffs`, `combat`, `physics`, `spawning`,
  `scoring`) and the `World`/entities and asserted each AC14–AC21 behaviour (32 assertions, all green).
- A **smoke-lifecycle harness** ran the real `App` smoke loop with `buffs.apply`/`buffs.tick`
  instrumented, proving the seeded Rapid pickup actually **collects @f2 → halves the cooldown to 6
  while active → expires @f61 → reverts** — i.e. AC20 verifies the *lifecycle*, not just exit 0.
- A **render harness** drew all 5 diamond pickups + 4 buff pills (+ shrink bars) + the shield bubble
  ring + the "+40" repair popup + START/GAME_OVER on a dummy SDL surface (the smoke run only ever
  renders a Rapid pill), confirming the v2 view paths don't raise.
(Scratch harnesses were removed after the run; commands + results are reproduced below.)

### Commands run
```
$env:SDL_VIDEODRIVER="dummy"; $env:SDL_AUDIODRIVER="dummy"
.\.venv\Scripts\python.exe workspace\game\main.py --smoke-test      # → exit 0  (run 3×: 0/0/0)
.\.venv\Scripts\python.exe -m game.main --smoke-test                # → exit 0  (package importable)
.\.venv\Scripts\python.exe -m compileall -q game                    # → OK     (no syntax/import warnings)
```
Environment: Python 3.14.3 · pygame-ce 2.5.7 (SDL 2.32.10) · Windows 11.

### Smoke-test gate (AC20) — preserved across the refactor + lifecycle exercised
```
pygame-ce 2.5.7 (SDL 2.32.10, Python 3.14.3)
smoke run 1 exit: 0
smoke run 2 exit: 0
smoke run 3 exit: 0
-m game.main exit: 0
Lifecycle: {'collected_frame': 2, 'expired_frame': 61, 'rapid_seen': True}   # cooldown seen while active: {6}
```
Exits 0 after exactly 120 headless frames; the §V2.5 seed (Rapid @(300,700), dur 60) is collected ~f2,
applies (cd 12→6), and expires ~f61 → reverts to cd 12 — full lifecycle well inside the 120-f cap. **PASS.**

### v2 acceptance-criteria checklist

| AC | Area | Result | Evidence |
|----|------|--------|----------|
| **AC14** | Spawn / drift / collect / no-penalty | ✅ PASS | bonus enters `world.bonuses`; drifts **+2.0 px/f** (flat, not ramp); ship overlap → collected & removed + buff applied; uncollected drifts off the bottom and is silently removed (HP unchanged, no buff). |
| **AC15** | Repair, no overheal | ✅ PASS | hp 30→70 (+40); from hp 80 → clamps to **100** (no overheal); instant — no timer stored; fires the "+40" popup. |
| **AC16** | Spread/rapid/shield change + revert | ✅ PASS | Fan: **1 beam → 3 beams** (±12°); Rapid: cooldown **12→6**; Shield: asteroid contact deals **0** dmg while active. All three **revert** on expiry (single shot / cd 12 / vulnerable). |
| **AC17** | HUD identifier + remaining-time | ✅ PASS | one pill per active timed buff (letter box + shrink bar, stable order F/R/S/2) drew without error; Repair shows only the transient "+40" popup, **no persistent pill** (REPAIR excluded from `TIMED_ORDER`). |
| **AC18** | Refresh / coexist / repair no-stack | ✅ PASS | re-collecting Fan at 380 f left resets to **full 480** (refresh, not 380+480); still 3 beams (no doubling); Fan+Rapid+Shield+Score coexist as independent timers; Repair at full HP stores nothing. |
| **AC19** | Restart cleanup | ✅ PASS | after `reset_run()`: `buff_timers=={}`, `bonuses==[]`, `repair_popup_timer==0`, hp/score/iframes back to baseline — no buff/timer/pickup leak. |
| **AC20** | Smoke 120 f + lifecycle | ✅ PASS | exit 0 after exactly 120 frames headless, 3× stable; smoke run **spawns→collects→applies→expires** the seeded Rapid (f2→f61) without raising. |
| **AC21** | Modular / imports clean / thin main | ✅ PASS | **21 files / 1222 lines** under `game/` (config/world/input/entities/systems/view + `app`); `main.py` is a **36-line** entry point; imports clean as script, `-m`, and `compileall`; **no line cap** (C3 retired). |

### v2 MUST → AC traceability (all met)
R23 pickup (drift/collect/no-penalty)→AC14 · R24 repair→AC15 · R25 fan / R26 rapid / R27 shield→AC16 ·
R28 spawning (drip + bullet-kill drop, reachable in smoke)→AC14/AC20 · R29 HUD→AC17 · R30 stacking/
refresh→AC18 · R31 expiry+restart→AC16/AC19 · R32 modular (cap retired)→AC21 · R33 smoke gate+lifecycle
→AC20. **COULD R34 Score×2** built & verified (award 50→**100** while active; covers kills + survival
tick). **R35 screen-clear** intentionally **not** built (deferred by GDD — absence does not fail v2).

### v1 regression check (no regression)
Re-ran the core v1 behaviours against the **new package** (not the old single file): AC3 clamp (ship
stays on-screen), AC5/AC9 asteroid contact −20 HP + i-frames, AC7/AC8 player bullet destroys asteroid +
awards score, AC10 hp≤0 → game-over trigger — all green. The v1 smoke gate (AC1/AC2) is the same gate
that passes above. The AC13 run-length caveat from the v1 report below is **unchanged and still
non-blocking** (the level-designer left the v1 ramp untouched in v2; bonuses are net survival-neutral
per `level_spec §V2`).

### Notes / observations (no action required)
- Buff model is clean and deterministic: every score gain flows through `scoring.award` (so Score×2 is
  uniform), invulnerability is `iframes>0 OR shield>0` (R27 overlap unambiguous), and the shield bubble
  ring only draws while shield is up — distinguishing a 5 s shield from a ~1 s i-frame blink.
- `reset_run()` correctly **preserves** `rng`/`stars`/`best` (session) while rebuilding everything
  run-specific — the intended R31 boundary, no leak observed.
- Dep direction respected: `config`/`world`/entities are pygame-free (which is what let QA drive the
  logic headlessly); only `app`/`input`/`view` touch pygame.

**Final verdict (v2): PASS.** R23–R33 + R34 verified; smoke gate green and now lifecycle-exercising;
modular package imports cleanly; no v1 regression. → hand off to **orchestrator** to declare v2 DONE.

---


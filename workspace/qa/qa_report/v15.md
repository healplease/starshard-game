# QA report — v15 (pytest suite + ruff/pyright tooling — port-then-delete the harness)

Date: 2026-06-07 · QA · Verdict: **PASS** · Scope: finish the migration off the 1,514-line
`qa/regression_harness.py` monolith — port the **32 e2e/behavioral checks** into `workspace/tests/e2e/`
per `test_plan.md` §2.4, prove ≥75-check parity on the full pytest suite, then **delete the monolith**
(zero coverage loss). Process change only — no game-feature change.

Verified against `qa/test_plan.md` §2 (the v15 test-infra contract: the §2.4 unit/e2e split, the §2.5 QA
gate, the §2.6 port-then-delete rule), the live `workspace/tests/conftest.py` + `unit/` lane (Programmer's
43 checks, backlog row 2), and the old `regression_harness.py` as the parity source.

## What I built — the 32 e2e checks (tests/e2e/, reusing conftest fixtures)
Mapped 1:1 from the harness's App/event-loop/render/balance checks into 7 files per the §2.4 contract.
Each reuses the shared `fresh_world` / `fonts` / `screen` / `tmp_save_path` fixtures; a small e2e-local
`conftest.py` adds an autouse `ensure_pygame` (re-inits pygame before each test, since `App.run()` calls
`pygame.quit()` — the monolith's per-check `ensure_pygame()`).

| File | Checks (count) |
|---|---|
| `test_app_lifecycle.py` | AC1, AC10, AC13, v9 balance, AC85 (5) |
| `test_pause_quit.py` | AC53, AC56, AC57, AC58, AC66, R76, AC61, AC62, AC63, AC67 (10) |
| `test_restart_hold.py` | R74, AC72, AC76, AC74, AC69 (5) |
| `test_render_smoke.py` | v9 render, AC68-render, AC77, v11 pulse, AC84 (5) |
| `test_layout.py` | AC47, AC68-arc-rects, AC71 (3) |
| `test_event_scripts.py` | v9 event (1) |
| `test_stats.py` | AC82r, AC81, nav (3) |
| **e2e total** | **32** |

## Gates (all green — §2.5)
- **Smoke gate (BLOCKING):** `main.py --smoke-test` → exit **0**; `-m game.main --smoke-test` → exit
  **0** (package entry point); `compileall -q game` → exit **0**, no warnings. ✅
- **Full pytest suite (BLOCKING — green AND ≥75):** `pytest workspace/tests` →
  **75 passed in 1.73 s, 0 failed**. Lane split confirmed by `--co`: **43 unit + 32 e2e = 75** —
  exactly the §2.4 contract totals. ✅

## Parity — old 75 ↔ new 75 (zero coverage loss, §2.6)
The monolith ran **75 `@test(...)` checks**; the pytest suite collects **75** (43 unit ported by the
Programmer in row 2 + 32 e2e ported here). Every harness check has a 1:1 pytest counterpart — same ACs,
same assertions, same real-code drive paths (combat/physics/buffs/bombs/encounter pipeline; real
`App._handle_events` / `_step_play` / `_restart_hold_step` / `run_event_script` / `balance_probe`;
real-surface blits + rect/arc geometry). No check was dropped, merged, or weakened. The v12-AC71
co-location assertion (the v14 fix) carries over intact in `test_layout.py::test_ac71_*`.

With parity proven, **`qa/regression_harness.py` is deleted** (the §2.6 hard rule: delete only after
the new suite passes ≥75). The remaining `regression_harness` mentions in the repo are doc/history
references, not code imports.

## Commands run (headless: `SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy`)
- `python -m pytest workspace/tests` → **75 passed / 0 failed**.
- `python -m pytest workspace/tests/unit --co` → 43 · `…/e2e --co` → 32.
- `python workspace/game/main.py --smoke-test` → exit **0**; `-m game.main --smoke-test` → exit **0**.
- `python -m compileall -q workspace/game` → exit **0**.

## Notes (non-blocking)
- Ruff/pyright are the Programmer's pre-handoff hygiene step (§2.5, non-blocking) and were not re-run
  here — this row added test files only, no `game/` code change. The two hard gates (smoke 0 + pytest
  green) are both met.

## Verdict
**PASS.** The 32 e2e checks are ported into `workspace/tests/e2e/` on the shared fixtures; the full
suite collects **75 (43 unit / 32 e2e)** and is green; both smoke invocations + compileall exit 0;
parity (old 75 ↔ new 75) is proven and `regression_harness.py` is deleted with zero coverage loss.
→ orchestrator (declare v15 DONE).

# QA report — v17 (HP-bar gradient + low-HP red vignette + non-green HEAVY pellet)

Date: 2026-06-07 · QA · Verdict: **PASS** · Scope: verify the three render-only v17 changes — (1) the HP bar
fades smoothly green→amber→red (no stepped jumps), (2) a subtle red vignette appears only below 25 % HP,
edge-only/center-clear, breathing, distinct from the v6 bomb flash, and (3) the HEAVY pellet is now orchid
-purple `#D230DC` (no longer green, clear of the HP green) — with **no AC1–AC93 regression**.

Verified against `art/art_spec/v17-hp-feedback-bullet-clarity.md` (V17.1–V17.5), the live `game/` package
(`config` v17 block + `EB_COLOR_PURPLE`/`EB_COLORS`, `view/hud.hp_bar_color`+`_draw_health_bar`,
`view/render.draw_low_hp_vignette`+`_draw_enemy_bullet`, `app._draw` render slot 8.5), and the new e2e tests.

## Gates (both hard gates green — test_plan §2.5)
- **Full pytest suite (BLOCKING — green AND ≥75):** `pytest workspace\tests` → **94 passed in 2.40 s, 0
  failed**. Suite grew **91 → 94 (+3)** = three QA-authored v17 e2e tests in `tests/e2e/test_render_smoke.py`;
  the ≥75 parity floor holds. ✅
- **Smoke gate (BLOCKING):** `main.py --smoke-test` → exit **0**; `-m game.main --smoke-test` → exit **0**
  (package entry, `PYTHONPATH=workspace`); `compileall -q workspace\game` → exit **0**, no warnings. ✅

## The three v17 behaviours — verdicts

| # | Behaviour | Verdict | Evidence |
|---|---|---|---|
| **V17.1** | HP bar = continuous green→amber→red, no stepped jumps | **PASS** | `hp_bar_color` anchors exact: hp 100→`(60,210,90)` green, 50→`(240,190,50)` amber, 0→`(230,60,60)` red; clamps (150→green, −10→red). Max colour delta per 1 HP over 0..100 = **4.12** (smooth); **no jump** at the retired thresholds (hp 39/40/41 and 19/20/21 each ~3-unit steps). Low HP (5) is red-dominant. `test_v17_hp_bar_gradient_continuous` |
| **V17.2** | Red vignette only <25 % HP, edge-only, breathing, ≠ flash | **PASS** | Not drawn at hp=25 (corner stays BG black); drawn at hp=24 (corner reddish). Baked surface: center `(300,400)` alpha **0** (clear), corner `(0,0)` alpha **110** = `VIGNETTE_MAX_ALPHA`, play-column `(300,700)` alpha **0**, corner RGB = `(230,60,60)` tint. Breathes: corner red 54↔99 over the 60-f pulse (**25 distinct values**). Distinct from v6 flash: red vs near-white (dist **271**), peak α110 vs α200, edge-only vs full-screen. Render slot **8.5** (below HUD, PLAY only) confirmed in `app._draw`. `test_v17_low_hp_vignette` |
| **V17.3** | HEAVY pellet orchid-purple `#D230DC`, not green/clash-free | **PASS** | `EB_COLOR_PURPLE == (210,48,220)`; `EB_COLORS["GREEN"]` → purple (family key kept as a spawn id); stale `EB_COLOR_GREEN` symbol **gone** (renamed). Drawn GREEN-family pellet body pixel = `(210,48,220)`. Dist to HP_GREEN/BONUS_REPAIR = **256** (clash gone). Nearest hues separated: BONUS_BOMB violet **65**, ENEMY magenta **54** (and further split by shape/role per spec V17.3.1). Shape/size unchanged: `PELLET_DRAW_R=8`, collision `EB_R=5`. `test_v17_heavy_pellet_purple` |

## No AC1–AC93 regression
- Full suite **94/94** green: all v1–v16 unit + e2e lanes pass (`test_boss`, `test_nova`, combat/buffs/bombs/
  save/strings/physics/world/enemies/imports + e2e lifecycle/pause-quit/restart/layout/stats/render-smoke).
- Existing render-smoke `test_render_smoke_all_states` already blits the GREEN pellet + PLAY HUD every run →
  the recolor + gradient draw paths ride the regression frame with no exception.
- Render-only change: zero new mechanic/economy/copy/collision/size; v6 flash (`draw_flash`) untouched and
  still drawn above the HUD; v1 §4.3 HP-bar rect/track/border draws unchanged (only the colour selector
  swapped for `hp_bar_color`).

## Commands run (headless: `SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy`, `STARSHARD_SAVE_PATH`→tmp)
- `python -m pytest workspace\tests` → **94 passed / 0 failed**.
- `python workspace\game\main.py --smoke-test` → exit **0**; `-m game.main --smoke-test` → exit **0**.
- `python -m compileall -q workspace\game` → exit **0**.
- QA probe (anchors/continuity, vignette trigger+bake+breathe+flash-distinction, pellet hue+clash distance)
  → all OK. Built as scratch, run, **then folded into three permanent e2e tests** (not left as scratch).
- `ruff check` on the new test → clean (hygiene, non-blocking).

## Verdict
**PASS.** The HP bar is a smooth 2-segment gradient with no stepped jumps; the low-HP vignette is red,
edge-only, center-clear, slow-breathing, gated strictly below 25 % HP, and visually distinct from the v6
bomb flash; the HEAVY pellet is orchid-purple `#D230DC`, far from the HP/Repair green it used to clash with,
with shape/size/collision intact. Both hard gates green (smoke exit 0 + pytest **94/94**); no AC1–AC93
regression. → orchestrator (declare v17 DONE).

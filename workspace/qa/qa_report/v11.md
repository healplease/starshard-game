# QA report — v11 (softer invulnerability pulse)

Date: 2026-06-05 · QA · Verdict: **PASS** · Scope: art-only render tweak (`_draw_player`)

Reuse-conscious scope call: v11 is a single-function visual change with no economy, requirements,
or state-machine surface. Verified via the smoke gate + the committed regression harness (which
includes a v11 test that reads the *real* alpha off the per-sprite surface) + a code read of
`_draw_player`. Did not re-run a bespoke full manual sweep — the harness already re-checks AC1–AC68.

## Gates
- **Smoke gate:** `--smoke-test` exits **0** after **120 f** (headless dummy SDL). ✅
- **Regression harness:** `59 checks — 59 PASS / 0 FAIL — ALL PASS` (58 prior AC1–AC68 + 1 new
  `v11/pulse`). No AC1–AC68 regression. ✅

## v11 contract (§V11.2–§V11.5) — verified
- **Cosine pulse 128↔255, 30-f period, driven by `blink_timer`** — the harness reads the actual
  alpha channel at `_PLAYER_SURF` centre: 255 at phase 0 (full cycle), 128 at phase 0.5 (half
  cycle). ✅
- **Never fully invisible** — alpha stays within [128, 255] across the whole 30-f cycle; floor is
  128, never 0. ✅
- **Smooth, not 2-state** — alpha takes >3 distinct values across one cycle (interpolated). ✅
- **Returns to solid the instant invuln ends** — `if not p.invulnerable` takes the cheap direct
  path; harness confirms the ship centre is drawn at full opaque `PLAYER_EDGE`. ✅
- **Shield bubble ring stays steady (no strobe)** — drawn straight to `screen` at full alpha every
  frame, *outside* the alpha surface (`render.py:185`), so it never pulses; the old early-return
  strobe is gone. ✅
- **SRCALPHA `set_alpha` no-op avoided** — alpha applied via `fill(..., BLEND_RGBA_MULT)`; the
  harness reading a partial alpha off the surface proves the channel is honoured (not a no-op). ✅
- **Per-sprite surface allocated once** — module-level `_PLAYER_SURF` with an `is None` guard; no
  per-frame allocation (only `fill`/draw/blit on the reused surface). ✅
- **No new palette colours** — only alpha is animated; `PLAYER`, `PLAYER_EDGE`, `BONUS_SHIELD`
  reused. ✅

## Verdict
All §V11.2–§V11.4 behaviour matches; smoke green; harness ALL PASS, no regression. **v11 PASS** →
orchestrator (declare DONE).

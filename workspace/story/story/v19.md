# v19 increment — precise controls (Focus mode control-hint copy)

Owner: writer · Date: 2026-06-07 · Status: spec ready
Inputs: `design/gdd/v19.md` (§V19.2 name "Focus", §V19.3 held-SHIFT feel), `art/art_spec/v19.md` (red hitbox
indicator), `requirements/requirements/v19.md` (R114, §V19.3 SHIFT-hint row), `story/v1-base.md` + `v6.md`
(the `CONTROLS_1` line this rewrites), the copy-surface map (`index.md`).

> One copy change: a control hint for the new **hold-SHIFT Focus** control. **No** new HUD label, popup, or
> screen text — the red hitbox circle (art_spec) + the visible slow-down teach the *effect* on use; copy only
> needs to teach the *key exists*. Matches the established rule that START is where every control is taught.

## V19.1 Decision — ADD a SHIFT/Focus hint (a `CONTROLS_1` rewrite)

**A hint is warranted.** Every other control (MOVE, fire, bomb, Esc-pause, Tab-stats, hold-Q-quit) is taught
on the START screen; SHIFT-to-Focus is a meaningful new in-flight control, so leaving it undocumented would be
the odd one out. It is **discoverability-only** — the mechanic is self-evident the instant you hold SHIFT (you
slow + the red hitbox circle appears), so the copy stays terse, just naming the key + action.

**Placement: woven into `CONTROLS_1`, right after the movement keys** — Focus is a *movement modifier*, so it
belongs in the in-flight action line next to MOVE (not on the `CONTROLS_2` "Esc Pause" meta line). No new
screen line / y-coordinate is added (no art-layout change); the existing y470 `CONTROLS_1` line just grows.

**Wording: `Hold Shift  Focus`** — matches the shipped held-gesture idiom verbatim (`Hold Q  Quit`,
`Hold R  Restart` = "Hold <key>" + double-space + one-word action), and uses the design name **Focus**
(§V19.2). The `·` joins it to the movement keys exactly as `·` joins `fire · bomb`.

## V19.2 The string

**`CONTROLS_1` ⚠ REWRITE** (config `CONTROLS_1`; delete the old literal in place, don't append a new const):

```python
# v19 ⚠ REWRITE (story §V19.2): add the hold-SHIFT Focus control next to MOVE (it's a
# movement modifier). Held-gesture idiom matches "Hold Q  Quit" / "Hold R  Restart".
CONTROLS_1 = "MOVE  Arrows / WASD  ·  Hold Shift  Focus      Z = fire · X = bomb"
```

- **Old (v6):** `"MOVE  Arrows / WASD      Z = fire · X = bomb"`
- **New:** `"MOVE  Arrows / WASD  ·  Hold Shift  Focus      Z = fire · X = bomb"`
- Reads as two clusters: **movement** (`MOVE  Arrows / WASD  ·  Hold Shift  Focus`) then **weapons**
  (`Z = fire · X = bomb`), the 6-space gap between them preserved from v6.

**Width check:** `FONT_SMALL` (`SysFont(None, 22)`) → **462 px** wide, centered in the 600 px window (≈69 px
margin each side). Comfortably inside the budget; balanced against the other centered START lines (TITLE 250 /
PITCH 320 / **CONTROLS_1 470** / CONTROLS_2 500 / STATS_HINT 530 / PROMPT 560 / QUIT_HINT 600). Single line, no
wrap.

## V19.3 What does NOT change (confirmed)
- **`CONTROLS_2`** (`"Esc  Pause"`), `START_STATS_HINT`, `START_QUIT_HINT`, `START_PROMPT`, `PITCH`, `TITLE` —
  untouched.
- **No PLAY HUD text** for Focus — the red hitbox circle (art_spec §V19a.2) is the on-screen indicator; there
  is no "FOCUS" label, meter, or status text (held modifier, not a pickup/state with a pill).
- **No popup** — Focus isn't collected; no `+…`-style transient.
- **No PAUSE/GAME_OVER copy change** — Focus is PLAY-only; the pause/game-over key lists are unaffected.
- **"Focus" stays a copy/design term only** — it is not a code identifier requirement; the Programmer names
  internals freely (the only blessed literal is the `CONTROLS_1` string above).

## V19.4 Copy-surface delta (for the map in `index.md`)
`CONTROLS_1` (START, `view/hud.py draw_start()` y470) — the only string touched this increment. No new
render site; no string removed.

## V19.5 Coverage
- R114 / §V19.3 SHIFT-hint row → **resolved: add the hint**, exact string in §V19.2, placement + width
  confirmed. Hands off to Level-designer (magnitudes); no further copy work pending for v19.

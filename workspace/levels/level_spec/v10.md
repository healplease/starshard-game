# v10 increment — Q-hold-to-quit on START + GAME_OVER — Level Spec

Owner: level-designer · Date: 2026-06-05 · Status: confirmed no-op
Inputs: `workspace/requirements/requirements/v10.md` (§38 Economy row, R82),
`workspace/design/gdd/v10.md` (§V10.8 smoke no-op), `workspace/levels/level_spec/v8-pause.md`
(the v8 no-op precedent — same gesture, same verdict), `workspace/shared/backlog.md` (v10 row 6).

---

## §V10.1 Verdict: confirmed economy no-op

v10 introduces **no changes** to the level / spawn economy. It extends the v8 Q-hold-to-quit
gesture to the **START** and **GAME_OVER** screens — both **UI-only states where the run isn't
advancing** (`w.frame` and every spawner are idle there). No spawn type, pickup kind, ramp delta,
or timing change. The v1–v7 economy contract is bit-for-bit preserved (and v8 already confirmed the
gesture itself is economy-neutral).

| Economy dimension | v10 change? | Evidence |
|---|---|---|
| **New spawn types** | None | START/GAME_OVER Q-hold adds zero entity classes; GDD §V10.9 lists **zero new design consts** (everything reuses the v8 set: `PAUSE_QUIT_FRAMES=30`, arc r/stroke, HP_AMBER/HP_BACK) |
| **New pickup kinds** | None | Pickup table stays the v6 6-kind ladder: Repair 30 / Fan 20 / Rapid 20 / Shield 12 / Score×2 12 / BOMB 6 (sums 100) — untouched |
| **Drip cadence** | Unchanged | `randint(600, 840) f` (v2-economy) — not touched |
| **Enemy-drop %** | Unchanged | 15 % per bullet-kill (v2-economy) — not touched |
| **On-screen pickup cap** | Unchanged | 3 (v2-economy) — not touched |
| **Difficulty ramp formulas** | Unchanged | v1-base.md §3 — not touched |
| **Enemy spawn mix / gates** | Unchanged | v5-spawn-mix.md §V5.1 (HEAVY @20 s / SCOUT @50 s) — not touched |
| **Bomb-pickup scarcity / lull** | Unchanged | `BOMB_SPAWN_LULL = 0`, weight 6 / 100 (v6) — not touched |
| **Boss breakpoint cadence** | Unchanged | TIME: first boss @75 s, then +90 s (v7-bosses.md §V7.1) — not touched |
| **Spawn-freeze/resume rule** | Unchanged | v7-bosses.md §V7.2 skip-no-bank / `BOSS_RESUME_LULL = 0` — not touched |
| **AC13 (1–3 min run)** | Protected | The gesture runs only in START + PAUSE + GAME_OVER; **PLAY is excluded (R81)** so `w.frame` never advances under it. The new reset-on-transition (R79) zeroes a UI counter only — no ramp/breakpoint timer is read or written. AC13 holds. |

---

## §V10.2 Edge case: none new beyond v8

The only timing the gesture touches is `App.q_hold_frames` — a **UI hold counter**, not a game
clock. It is reset to 0 on every state transition (GDD §V10.4) and is never consulted by any
spawner, ramp formula, or boss breakpoint. The v8 §V8.2 real-world-vs-in-game-time ruling already
covers PAUSE; START and GAME_OVER don't advance `w.frame` at all, so there is nothing further to
rule on here.

---

## §V10.3 Traceability

| Requirement / AC | Coverage |
|---|---|
| R82 — smoke + render-smoke preserved, no economy change | Confirmed: GDD §V10.8 — smoke never holds Q (no-op on the scripted path), zero new seeding / SMOKE_TIMELINE row / frame-budget change |
| §38 Economy row — Level-designer confirm/skip | This file — confirmed no-op |
| AC13 — run length 1–3 min | Protected: gesture excluded from PLAY (R81); UI counter only; ramp/breakpoints unchanged |
| All prior level-spec contracts (v1–v8) | Unchanged; this file is additive only |

No new levers, no new constants, no new spawn numbers. This file exists solely to satisfy the v10
backlog row 6 confirmation requirement. The locked economy from v7-bosses.md remains the current
production contract.
